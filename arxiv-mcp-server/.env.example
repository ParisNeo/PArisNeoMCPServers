# PArisNeoMCPServers/arxiv-mcp-server/.env.example

# The root directory where all Arxiv databases (subfolders) will be stored.
# Each database will be a subfolder in this path, containing downloaded PDFs and metadata JSONs.
# If the path is relative, it's relative to the `arxiv-mcp-server` project root.
# Default will be '../mcp_example_outputs/arxiv_databases' if this is not set.
ARXIV_DATABASES_ROOT="../mcp_example_outputs/arxiv_databases"