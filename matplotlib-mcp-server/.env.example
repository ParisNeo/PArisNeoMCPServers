# PArisNeoMCPServers/matplotlib-mcp-server/.env.example

# Optional: Default Dots Per Inch for generated plots.
# MPL_DEFAULT_DPI=100

# Optional: Default image format for generated plots (e.g., png, jpg, svg).
# 'png' is generally a good default for web and general use.
# 'svg' is good for scalable vector graphics.
# MPL_DEFAULT_FORMAT="png"

# Optional: Default Matplotlib backend.
# 'Agg' is a good non-interactive backend for generating files.
# It's usually set programmatically, but could be an override.
# MPL_BACKEND="Agg"