# PArisNeoMCPServers/duckduckgo-mcp-server/.env.example
# Example environment variables for the DuckDuckGo MCP Server

# Default number of search results to return if not specified in the tool call.
# DDG_DEFAULT_MAX_RESULTS=5

# Default search region if not specified in the tool call.
# 'wt-wt' is worldwide. Other examples: 'us-en' (US English), 'de-de' (Germany German).
# See https://duckduckgo.com/params or search "duckduckgo regions" for a list.
# DDG_DEFAULT_REGION="wt-wt"

# Note: The server will use sensible defaults (5 results, 'wt-wt' region)
# if these environment variables are not set.